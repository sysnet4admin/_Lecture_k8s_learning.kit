apiVersion: batch/v1
kind: Job
metadata:
  name: jobs-completions 
spec:
  completions: 3
  template:
    spec:
      containers:
      - name: jobs-completions
        image: sysnet4admin/net-tools
        command: ["curlchk",  "nginx"]
      restartPolicy: Never
